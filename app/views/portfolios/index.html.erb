<div class="album text-muted">
  <div class="container">
    <%= link_to "Create a New Portfolio Item", new_portfolio_path, class: "create-item" if logged_in?(:site_admin) %>
    <div class="row sortable">

      <%= render partial: "portfolio_item", collection: @portfolio_items %>
      
    </div>
  </div>
</div>